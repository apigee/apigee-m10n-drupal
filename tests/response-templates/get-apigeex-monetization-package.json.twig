{#
/**
 * @file
 *   - GET /v1/organizations/{org}/apiproducts
 *   - GET /v1/organizations/{org}/developers/{developer}/apiproducts
 *
 * Response Code: 200
 *
 * Variables:
 * - packages: The packages to include.
 */
#}
{
    "apiProduct" : [
        {% for package in xproducts %}
            {% include 'xproduct.json.twig' %}{{ loop.last ? '' : ',' }}
        {% endfor %}
    ]
}
