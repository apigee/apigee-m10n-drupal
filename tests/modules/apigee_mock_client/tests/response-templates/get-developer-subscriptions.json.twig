{#
/**
 * @file
 *   GET /v1/mint/organizations/{org_name}/developers/{developer_id}/developer-accepted-rateplans
 *
 * Response Code: 200
 */
#}
{
    "developerRatePlan": [
        {% for subscription in subscriptions %}
            {% include 'subscription.json.twig' %}{{ loop.last ? '' : ',' }}
        {% endfor %}
    ],
    "totalRecords": {{ subscriptions|length }}
}
